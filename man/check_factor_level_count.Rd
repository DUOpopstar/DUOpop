% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-utility_evaluation.R
\name{check_factor_level_count}
\alias{check_factor_level_count}
\title{Check Consistency of Factor Level Counts Between Datasets}
\usage{
check_factor_level_count(syn_object, data_observed)
}
\arguments{
\item{syn_object}{A list containing a synthetic dataset under the element \code{syn}.}

\item{data_observed}{A data frame containing the original observed data.}
}
\value{
Returns \code{NULL} invisibly if all factor levels are consistent; otherwise, it stops with an error message.
}
\description{
Compares the number of levels for all factor variables between an observed dataset and a synthetic dataset contained within a \code{syn_object}.
If discrepancies are found, it throws an error listing the variables with inconsistent levels.
}
\examples{
\dontrun{
# Create example observed data
data_observed <- data.frame(
  var1 = factor(c("A", "B", "C")),
  var2 = factor(c("D", "E", "F"))
)

# Create a synthetic dataset with matching factor levels
syn_object <- list(
  syn = data.frame(
    var1 = factor(c("A", "B", "C")),
    var2 = factor(c("D", "E", "F"))
  )
)

# This should pass without errors
check_factor_level_count(syn_object, data_observed)

# Modify the synthetic dataset to have inconsistent factor levels
syn_object$syn$var2 <- factor(c("D", "E", "E"))

# This will throw an error
check_factor_level_count(syn_object, data_observed)
}
}
\keyword{internal}
